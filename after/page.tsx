//Fragmented the UI into selective components to make it more readable
import React from 'react';
import BurnPage from './BurnPage';

const Page = () => {
    return (
        <div>

            <BurnPage />

        </div>
    );
};

export default Page;
